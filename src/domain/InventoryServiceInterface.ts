import Invenctory from "@/model/invenctory/InvectoryInterface";
import {AxiosResponse} from "axios";

export default interface InvenctoryServiceInterface {
    // details(id: number): Promise<any>,
    // details(id: number) : Promise<Invenctory[]>
}